{
  "name": "marketdock-app",
  "version": "1.0.0",
  "private": true,
  "description": "MarketDock native app wrapper (Capacitor) — displays the live MarketDock website inside a native Android shell.",
  "scripts": {
    "sync": "cap sync",
    "open:android": "cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/app": "^6.0.3",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "@capacitor/cli": "^6.1.2"
  }
}
